# Core dependencies
transformers==4.30.0
torch==2.0.1
sentencepiece==0.1.99
accelerate==0.21.0

# OpenAI/Gradio dependencies
openai==1.3.6
gradio==3.44.0
python-dotenv==1.0.0

# Additional utilities
tqdm==4.65.0
numpy==1.24.3
pandas==2.0.2
requests==2.31.0

# CPU-only version of PyTorch (use this if no GPU available)
# torch==2.0.1+cpu --extra-index-url https://download.pytorch.org/whl/cpu
